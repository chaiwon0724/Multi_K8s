FROM node:6             # Use an official Node runtime as the parent image
WORKDIR /app           # Set the working directory in the container to /app
ADD . /app                 # Copy the current directory contents into the container at /app
EXPOSE 80                  # Make the container's port 80 available to the outside world
CMD ["node", "app.js"]  # Run app.js using node when the container launches
